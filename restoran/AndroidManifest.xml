<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.proje.restoran">
<uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.DeviceDefault">
        <activity android:name=".main">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    <activity android:name="admin" android:label="@string/app_name"></activity>
    <activity android:name="kasa_1" android:label="@string/app_name"></activity>
    <activity android:name="kasa_2" android:label="@string/app_name"></activity>
    <activity android:name="mutfak" android:label="@string/app_name"></activity>
    <activity android:name="adisyon_1" android:label="@string/app_name"></activity>
    <activity android:name="adisyon_2" android:label="@string/app_name"></activity>
    </application>

</manifest>
